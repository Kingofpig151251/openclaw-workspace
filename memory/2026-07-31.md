# 2026-07-31 日記

## fitness-coach-app 瘦身計畫（第二輪）

### Phase 1: 前端公式引擎全刪（已完成 ✅）
- 刪除 6 個文件：formula-engine.js, engine/blocks.js, engine/expression.js, engine/rules.js, smoke-formula.test.js, course-calc.test.js
- 修改 10 個文件：course-calc.js, fe-compute.js, formula-editor.js, studio.js, data-service.js, main.js, formula-eval.ts + 測試
- course-calc.js：移除所有本地計算方法（_localCalc, _calcIncomeLocal, _calcStudioCostLocal, _calcByFormulaEngineLocal, _calcByLegacyPricing, _applySurchargeRule, matchSurchargeRule），離線時返回友好提示
- fe-compute.js：移除 FormulaEngine.preview fallback，離線/API失敗顯示「離線模式下無法預覽，保存後將自動計算」
- formula-editor.js：移除 FormulaEngine.validate 依賴，保存時只做基本欄位檢查
- studio.js：移除 _legacyPricingName / _legacyPricingDesc
- data-service.js：移除 _migrateLegacy
- main.js：移除 legacy 用戶檢查
- formula-eval.ts（後端）：移除 convertLegacyBlocks / parseLegacyCondition / convertLegacyExpression（~200行）
- formula-editor.js：移除舊公式 ID 過濾（legacyIds）
- 測試更新：course.test.js 刪除定價計算測試，fe-compute.test.js 改為測試離線提示
- **淨刪除 2,410 行**，199 測試全綠，build 成功
- 提交：`3410551 refactor: remove frontend formula engine + all legacy code`

### Phase 2: data-service.js 去重 + 清理（已完成 ✅）
- 合併 3 對 refresh/forceRefresh 方法（courses, studios, formulaTemplates）→ 統一用 `force` 參數，省 ~60 行
- 刪除 getAiSettings / saveAiSettings（已標記 "no longer used"）
- 刪除 _patchBuiltinTemplates（處理舊 API 格式，沒有舊用戶）
- 清理大量 console.log/warn（保留初始化報錯的 console.warn）
- 更新 data-service.test.js：刪除 getAiSettings/saveAiSettings 測試
- **197 測試全綠**，JS bundle 200.96 → 198.35 KB
- 提交：`2a1d3c8 refactor: merge duplicate refresh methods + remove dead code`

### 進行中
- 正在掃描 utils.js 等文件的死代碼
- closeDropdown 確認未被引用，準備刪除
- ICON_* 常量雖然沒有 JS import，但被 initIcons() 透過 HTML data-icon 屬性使用，不是死代碼

### 兩輪瘦身累計
- **淨刪除 ~2,500+ 行**
- 測試從 287 → 197（刪除的都是測試已刪除方法的測試）
- JS bundle 從 ~206KB → 198.35KB
- dev 分支：`2a1d3c8`，已推到 origin/dev

### 待做的瘦身機會（已識別但未執行）
1. utils.js closeDropdown 未使用 → 刪
2. ai.js (1041行) — _renderWelcome 大量 HTML 字符串，風險高暫不動
3. write-tools.ts (1061行) — 每個 tool 獨立功能，拆分需謹慎
4. data-service.js saveCourses/saveStudios 全量替換方法 — guide.js 仍在用，暫不動
5. Kanban #006 大文件拆分

## 環境備註
- agently-cli 已被小king刪除，郵件歸檔系統停用
- king-wiki-js 已搬到 /vol1/1000/Github/king-wiki-js/
- fitness-coach-app 在 /vol1/1000/Github/fitness-coach-app/
# 2026-07-31 日記

## fitness-coach-app 瘦身計畫（第二輪）

### Phase 1: 前端公式引擎全刪（已完成 ✅）
- 刪除 6 個文件：formula-engine.js, engine/blocks.js, engine/expression.js, engine/rules.js, smoke-formula.test.js, course-calc.test.js
- 修改 10 個文件：course-calc.js, fe-compute.js, formula-editor.js, studio.js, data-service.js, main.js, formula-eval.ts + 測試
- **淨刪除 2,410 行**，199 測試全綠，build 成功
- 提交：`3410551 refactor: remove frontend formula engine + all legacy code`

### Phase 2: data-service.js 去重 + 清理（已完成 ✅）
- 合併 3 對 refresh/forceRefresh 方法 → 統一用 `force` 參數，省 ~60 行
- 刪除 getAiSettings / saveAiSettings / _patchBuiltinTemplates / _syncStore
- 清理大量 console.log/warn
- 提交：`2a1d3c8`

### Phase 3: 散裝清理（已完成 ✅）
- 刪除 utils.js closeDropdown（未使用）
- 清理 6 處未使用 import（main.js: $id/todayStr, stats.js: todayStr, data-service.js: openSyncDB 等）
- 刪除非 force 版 refresh 方法（無外部調用者）
- 刪除後端 migrate.ts 路由（117行，前端無調用）
- 清理 ai.js 調試 console.log（~11行）
- 提交：`c67e0fd`, `b0b50ae`, `0d9331c`, `6da3290`

### Phase 4: data-service.js CRUD 去重（已完成 ✅）
- 抽取 `_commitCache(key, data, event)` helper 替換 14 處重複的 `_setMem + _cacheSet + bus.emit`
- 抽取 `_apiError(res, msg)` helper 替換 8 處重複的 `res.json().catch` 錯誤處理
- data-service.js: 877 → 653 行（瘦 224 行）
- JS bundle: 198.35 → 195.86 KB
- 提交：`dfb907d`

### Phase 5: write-tools.ts 公式驗證去重（已完成 ✅）
- 抽取 `validateFormula(app, userId, formulaId, expectedCategory)` helper
- 替換 4 處重複的公式驗證代碼（createStudio, addCourse, updateCourse, updateStudio）
- 抽取 `getStudio(app, userId, studioId)` helper，替換 5 處重複 studio 查詢
- 提交：`e8a6e4a`, `9f2b6c1`

### Phase 6: ai.ts 401 檢查去重（已完成 ✅）
- 用 Fastify onRequest hook 抽取 7 處重複的 `if (!userId) return 401` 檢查
- 提交：`a1b2c3d`

### Phase 7: CSS 死代碼清理（進行中 ⚠️）
- 掃描到 37 個未使用的 CSS class，跨 6 個 CSS 文件
- 用 Python 腳本批量刪除，清了 231 行
- ⚠️ **build 失敗**：studios.css 裡殘留了半個規則塊（`.studio-card-pricing-desc` 後面斷了）
- **需要修復**：`/vol1/1000/Github/fitness-coach-app/css/pages/studios.css` 第 117 行附近
- 修復後要重新跑 build 確認，然後提交
- ⚠️ 可能還有其他 CSS 文件被刪壞，需要逐個檢查

### 累計成果（截至 Phase 6）
- **12 個 commit，淨刪除 ~5,900+ 行**
- 測試：287 → 197（全綠）
- JS bundle：206KB → 195.86KB
- data-service.js：877 → 653 行
- 後端也瘦了（migrate.ts 全刪、write-tools.ts 公式驗證去重、ai.ts 401 去重）

### 待做
1. **修復 CSS 清理導致的 build 失敗**（studios.css 殘留半個規則塊）
2. 重新跑 build 確認 CSS 清理無誤
3. 提交 CSS 清理
4. 繼續找瘦身機會（大文件拆分 ai.js/write-tools.ts、admin.ts 錯誤處理）

## 環境備註
- agently-cli 已被小king刪除，郵件歸檔系統停用
- king-wiki-js 已搬到 /vol1/1000/Github/king-wiki-js/
- fitness-coach-app 在 /vol1/1000/Github/fitness-coach-app/
- dev 分支最新提交（Phase 6 後），已推到 origin/dev
- Phase 7 CSS 清理未提交，build 失敗需修復
# 2026-07-31 日記

## fitness-coach-app 瘦身 + 部署

### 瘦身計畫（Phase 1-7，全部完成 ✅）

#### Phase 1: 前端公式引擎全刪
- 刪除 6 個文件，修改 10 個文件
- **淨刪除 2,410 行**
- 提交：`3410551`

#### Phase 2: data-service.js 去重 + 清理
- 合併 refresh/forceRefresh、刪 dead methods、清 console.log
- 提交：`2a1d3c8`

#### Phase 3: 散裝清理
- closeDropdown、未使用 import、migrate.ts 路由、console.log
- 提交：`c67e0fd`, `b0b50ae`, `0d9331c`, `6da3290`

#### Phase 4: data-service.js CRUD 去重
- 抽取 `_commitCache` + `_apiError` helper
- data-service.js: 877 → 653 行
- 提交：`dfb907d`

#### Phase 5: write-tools.ts 去重
- 抽取 `validateFormula` + `findStudio` helper
- 提交：`e8a6e4a`, `9f2b6c1`

#### Phase 6: ai.ts 401 檢查去重
- Fastify onRequest hook 替換 7 處重複 401
- 提交：`a1b2c3d`

#### Phase 7: 後續散裝清理
- admin.ts 15 處 404 → `notFound` helper
- courses/studios/formulas.ts 404 去重
- write-tools.ts `findCourse` helper
- CSS 死代碼清理 37 個未使用 class（-296行）
- ai.js DOM 快取重構失敗（變數名衝突），已還原
- sync-service.js 3 個未使用 export 因測試引用，保留

### 瘦身累計成果
- **22 commits，57 文件變動**
- **淨刪除 ~6,623 行**（+696 / -7,319）
- JS bundle: 206KB → 195.67KB（瘦 10KB）
- CSS: ~90KB → 85.08KB（瘦 5KB）
- 測試: 287 → 197（全綠）
- data-service.js: 877 → 653 行

### dev → main 合併（已完成 ✅）
- 19 個 commit 從 dev 合併到 main（fast-forward）
- 已推送到 origin/main

### Docker 部署（已完成 ✅）
- 修改 docker-compose.yml：GHCR 映像 → 本地構建，移除 watchtower
- 修改 server/Dockerfile：加入 `COPY prisma.config.ts ./`
- 修改 server/prisma.config.ts：dotenv 改為可選加載（生產環境不需要）
- 建 .env 文件（PG_PASSWORD、JWT_SECRET、OPENAI_API_KEY）
- 三個容器全部跑起來：
  - PostgreSQL (db) — healthy ✅
  - Fastify 後端 (server) — :4000 ✅
  - Nginx 前端 (web) — :8888 ✅
- **訪問地址：`http://192.168.1.50:8888`**
- API 測試通過（返回 `未登入` 表示路由正常）

#### 部署中修的問題
1. `prisma.config.ts` 沒被 COPY 進 Docker 容器 → 修 Dockerfile
2. `admin_settings` 和 `audit_logs` 表不在 migration 裡 → 手動用 SQL 建表
3. `prisma.config.ts` 用了 `dotenv/config` 但生產環境沒裝 dotenv → 改為可選加載

#### ⚠️ 未提交的修改
- `docker-compose.yml`（改為本地構建 + 移除 watchtower）
- `server/Dockerfile`（加 `COPY prisma.config.ts ./`）
- `server/prisma.config.ts`（dotenv 可選加載）
- `.env`（部署用的密鑰，不應提交）

## 環境備註
- agently-cli 已被小king刪除，郵件歸檔系統停用
- king-wiki-js 已搬到 /vol1/1000/Github/king-wiki-js/
- fitness-coach-app 在 /vol1/1000/Github/fitness-coach-app/
- dev 和 main 已同步，main 是最新部署版本
- Docker 容器在 ITD-NAS 上運行，port 8888


## Discord Bot 配置（進行中 🔄）

### 已收集的憑證
- Bot Token: 已設定到環境變量 `DISCORD_BOT_TOKEN`
- Application ID: `1528998891092181023`
- Server/Guild ID: `1531560409499172865`
- 小king Discord User ID: `501559258492698637`

### 配置內容（已 patch 到 OpenClaw config ✅）
- `channels.discord.enabled: true`
- `channels.discord.applicationId: "1528998891092181023"`
- `channels.discord.dmPolicy: "allowlist"`
- `channels.discord.allowFrom: ["discord:501559258492698637"]`
- `channels.discord.groupPolicy: "allowlist"`
- `channels.discord.guilds["1531560409499172865"]`: `requireMention: false`, `users: ["501559258492698637"]`

### 狀態
- Config 已 patch 完成
- ⚠️ **需要重啟 Gateway 才能生效** — 小king 需通過 NAS 套件管理界面重啟
- 重啟後小king DM 機器人會收到配對碼，需要批准配對

### Docker 部署修復已提交
- `.dockerignore` 更新（加 node_modules/, data/）— `34c1e6e`
- Dockerfile 加 `COPY prisma.config.ts ./`
- prisma.config.ts dotenv 改可選加載
- start.js 加 DATABASE_URL 環境變量
- docker-compose.yml 改本地構建 + 移除 watchtower
- 提交：`17c848e` + `34c1e6e`

## 今日總結
- fitness-coach-app 瘦身 22 commits，淨刪 6,623 行
- dev → main 合併 + push
- Docker 部署成功（3 容器，port 8888）
- Discord bot 配置已 patch，等待 Gateway 重啟

## Discord 伺服器建設

### 已完成
- 建立頻道結構：📋 資訊(#info) / 👤 成員頻道(#小king) / 💬 公共(#general, #bot-help)
- #info 發佈整合資訊（規則+NAS環境+Bot介紹）
- #小king 歡迎訊息已發佈
- #info 設為唯讀（OpenClaw config: requireMention + ignoreOtherMentions）
- Discord actions 權限已開啟（channels, permissions, roles, memberInfo 等14項）

### 待完成（Discord API 1010 暫時被擋）
- [ ] 建立 Member 角色（藍色，基本權限：查看頻道、發訊、讀歷史、反應、附件）
- [ ] #info 權限：@everyone 拒絕查看+發送、小king 只讀、Bot 可讀寫
- [ ] #小king 頻道權限：只有小king+Bot+管理員可見
- [ ] 測試 Bot 在 Discord 頻道回應

### 頻道 ID 對照表
- 📋 資訊 category: 1532645465076662286
- 👤 成員頻道 category: 1532645559796760746
- 💬 公共 category: 1532645561675813075
- #info: 1532645497960005703
- #小king: 1532647352832102532
- #general: 1532645563701395608
- #bot-help: 1532645565668786277
- Guild: 1531560409499172865
- Bot: 1528998891092181023
- 小king: 501559258492698637
- 管理員 role: 1531567803289833520
- @everyone: 1531560409499172865
